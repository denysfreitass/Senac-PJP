# Leia preço (float) e fator (float). Faça preco *= fator.

preco = float(input("Digite o preço do produto: "))
fator = float(input("Digite o valor do fator: "))

preco *= fator 
print(f"O preço após o fator fica {preco}")